"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ConfigurationWireHelper = void 0;
const api_endpoints_1 = require("../api-endpoints");
const http_client_1 = require("../http-client");
const sdk_token_decoder_1 = require("../sdk-token-decoder");
const configuration_wire_types_1 = require("./configuration-wire-types");
/**
 * Helper class for fetching and converting configuration from the Eppo API(s).
 */
class ConfigurationWireHelper {
    /**
     * Build a new ConfigurationHelper for the target SDK Key.
     * @param sdkKey
     * @param opts
     */
    static build(sdkKey, opts = { sdkName: 'js-client-sdk', sdkVersion: '4.0.0' }) {
        const { sdkName, sdkVersion, baseUrl, fetchBandits } = opts;
        return new ConfigurationWireHelper(sdkKey, sdkName, sdkVersion, baseUrl, fetchBandits);
    }
    constructor(sdkKey, targetSdkName = 'js-client-sdk', targetSdkVersion = '4.0.0', baseUrl, fetchBandits = false) {
        this.fetchBandits = fetchBandits;
        const queryParams = {
            sdkName: targetSdkName,
            sdkVersion: targetSdkVersion,
            apiKey: sdkKey,
            sdkProxy: 'config-wire-helper',
        };
        const apiEndpoints = new api_endpoints_1.default({
            baseUrl,
            queryParams,
            sdkTokenDecoder: new sdk_token_decoder_1.default(sdkKey),
        });
        this.httpClient = new http_client_1.default(apiEndpoints, 5000);
    }
    /**
     * Fetches configuration data from the API and build a Bootstrap Configuration (aka an `IConfigurationWire` object).
     * The IConfigurationWire instance can be used to bootstrap some SDKs.
     */
    async fetchConfiguration() {
        // Get the configs
        let banditResponse;
        const configResponse = await this.httpClient.getUniversalFlagConfiguration();
        if (!configResponse?.flags) {
            console.warn('Unable to fetch configuration, returning empty configuration');
            return Promise.resolve(configuration_wire_types_1.ConfigurationWireV1.empty());
        }
        const flagsHaveBandits = Object.keys(configResponse.banditReferences ?? {}).length > 0;
        if (this.fetchBandits && flagsHaveBandits) {
            banditResponse = await this.httpClient.getBanditParameters();
        }
        return configuration_wire_types_1.ConfigurationWireV1.fromResponses(configResponse, banditResponse);
    }
}
exports.ConfigurationWireHelper = ConfigurationWireHelper;
//# sourceMappingURL=configuration-wire-helper.js.map