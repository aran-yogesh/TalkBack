import Event from './event';
import NamedEventQueue from './named-event-queue';
export default class BatchEventProcessor {
    private readonly eventQueue;
    private readonly batchSize;
    constructor(eventQueue: NamedEventQueue<Event>, batchSize: number);
    nextBatch(): Event[];
    push(...events: Event[]): void;
    isEmpty(): boolean;
}
//# sourceMappingURL=batch-event-processor.d.ts.map