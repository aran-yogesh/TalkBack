import { Variation } from './interfaces';
import { FlagKey } from './types';
export interface OverridePayload {
    browserExtensionKey: string;
    overrides: Record<FlagKey, Variation>;
}
export declare const sendValidationRequest: (browserExtensionKey: string) => Promise<void>;
export declare class OverrideValidator {
    private validKeyCache;
    parseOverridePayload(overridePayload: string): OverridePayload;
    private validateParsedOverridePayload;
    validateKey(browserExtensionKey: string): Promise<true | undefined>;
}
//# sourceMappingURL=override-validator.d.ts.map