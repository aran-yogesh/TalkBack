"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.bootstrapConfigCommand = void 0;
const fs = require("fs");
const configuration_wire_helper_1 = require("../../configuration-wire/configuration-wire-helper");
exports.bootstrapConfigCommand = {
    command: 'bootstrap-config',
    describe: 'Generate a bootstrap configuration string',
    builder: (yargs) => {
        return yargs.options({
            key: {
                type: 'string',
                description: 'SDK key',
                alias: 'k',
                default: process.env.EPPO_SDK_KEY,
            },
            sdk: {
                type: 'string',
                description: 'Target SDK name',
                default: 'js-client-sdk',
            },
            'base-url': {
                type: 'string',
                description: 'Base URL for the API',
            },
            output: {
                type: 'string',
                description: 'Output file path',
                alias: 'o',
            },
        });
    },
    handler: async (argv) => {
        if (!argv.key) {
            console.error('Error: SDK key is required');
            console.error('Provide it either as:');
            console.error('- Command line argument: --key <sdkKey> or -k <sdkKey>');
            console.error('- Environment variable: EPPO_SDK_KEY');
            process.exit(1);
        }
        try {
            const helper = configuration_wire_helper_1.ConfigurationWireHelper.build(argv.key, {
                sdkName: argv.sdk,
                baseUrl: argv['base-url'],
                fetchBandits: true,
            });
            const config = await helper.fetchConfiguration();
            if (!config) {
                console.error('Error: Failed to fetch configuration');
                process.exit(1);
            }
            const jsonConfig = JSON.stringify(config, null, 2);
            if (argv.output && typeof argv.output === 'string') {
                fs.writeFileSync(argv.output, jsonConfig);
                console.log(`Configuration written to ${argv.output}`);
            }
            else {
                console.log('Configuration:');
                console.log('--------------------------------');
                console.log(jsonConfig);
                console.log('--------------------------------');
            }
        }
        catch (error) {
            console.error('Error fetching configuration:', error);
            process.exit(1);
        }
    },
};
//# sourceMappingURL=bootstrap-config.js.map