import { NamedEventQueue } from '@eppo/js-client-sdk-common';
/** A localStorage-backed NamedEventQueue. */
export default class LocalStorageBackedNamedEventQueue<T> implements NamedEventQueue<T> {
    readonly name: string;
    private readonly localStorageKey;
    private eventKeys;
    constructor(name: string);
    splice(count: number): T[];
    isEmpty(): boolean;
    get length(): number;
    push(event: T): void;
    [Symbol.iterator](): IterableIterator<T>;
    shift(): T | undefined;
    private loadStateFromLocalStorage;
    private saveStateToLocalStorage;
    private generateEventKey;
    private hashEvent;
}
//# sourceMappingURL=local-storage-backed-named-event-queue.d.ts.map